fastapi==0.110.0
uvicorn==0.27.0
pydantic==2.6.0
httpx==0.27.0
prometheus-client==0.20.0
pytest==8.0.0
# pytest-asyncio is not needed -- the autograder tests are sync (TestClient
# + plain pytest). Was previously pinned at 0.25.0 which requires pytest>=8.2
# and broke `pip install -r requirements.txt`.
pyyaml==6.0.1
